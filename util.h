/** @file 
 *  @brief Utility functions */

#ifndef UTIL_H
#define UTIL_H

/** Copy the string and allocate it on the heap.
  * @param str The string to be copied (can be const).
  * @returns a pointer to the copy of the string.
*/
char *copied_string(char *str);



#endif
